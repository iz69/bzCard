server {
  listen 5173;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /bzcard/ {
    rewrite ^/bzcard/(.*)$ /$1 break;
    try_files $uri $uri/ /index.html;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
